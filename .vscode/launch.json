{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run simple",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/target/debug/simple",
            "args": [],
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "cargo build"
        }
    ]
}